#ifndef HEADER_quiz_Score_hpp_ALREADY_INCLUDED
#define HEADER_quiz_Score_hpp_ALREADY_INCLUDED

#include <map>

namespace quiz { 

    class Score
    {
    public:
        std::map<std::string, int> team__score;
    };

} 

#endif
